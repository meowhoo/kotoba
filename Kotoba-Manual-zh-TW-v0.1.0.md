# Kotoba 使用手冊

**版本**: 0.1.0
**更新日期**: 2025-11-09
**作者**: Justin Chiu <meowhoo@gmail.com>

---

## 📋 目錄

1. [關於本軟體](#1-關於本軟體)
2. [系統需求](#2-系統需求)
3. [安裝步驟](#3-安裝步驟)
4. [首次設定](#4-首次設定)
5. [功能詳解](#5-功能詳解)
   - 5.1 [書庫管理](#51-書庫管理)
   - 5.2 [抓圖設定](#52-抓圖設定)
   - 5.3 [OCR 辨識](#53-ocr-辨識)
   - 5.4 [閱讀模式](#54-閱讀模式)
   - 5.5 [翻譯設定](#55-翻譯設定)
   - 5.6 [單字卡系統](#56-單字卡系統)
6. [快捷鍵參考](#6-快捷鍵參考)
7. [常見問題](#7-常見問題)
8. [疑難排解](#8-疑難排解)

---

## 1. 關於本軟體

Kotoba 是一款多國語言學習工具。支援多國語言的文字辨識，搭配 AI 翻譯與智慧單字卡系統，讓您輕鬆閱讀外文書籍、累積詞彙，打造個人化的語言學習環境。

### 主要功能

- **🔍 多語言 OCR 辨識**：支援多國語言文字辨識，自動處理多欄排版，智慧判定直排/橫排
- **🤖 多 LLM 翻譯**：整合 OpenAI、Claude、Gemini、Grok，可自訂翻譯風格、角色語氣與名詞對照表
- **📖 智慧閱讀模式**：三種閱讀模式（純圖、左圖右譯、OCR 文字），中日文自動直排顯示
- **💬 AI 單字卡系統**：透過 LLM 自動標註發音、翻譯與例句，支援跨書籍索引與 Anki 匯出
- **🎨 完整主題與多語言界面**：Light/Dark/System 主題，繁中/英文雙語支援

### 適用對象

- 語言學習者，想累積詞彙、建立個人化單字庫
- 需要 AI 翻譯輔助來閱讀外文書籍
- 想將書籍內容翻譯成任何你需要的語言

---

## 2. 系統需求

### 最低需求

- **作業系統**: Windows 10/11 (64-bit)
- **記憶體**: 最低 4GB
- **硬碟空間**: 約 2GB
- **網路**: 需要網路連線（用於 OCR 和 AI 翻譯）

### 必要服務

1. **Google Cloud Vision API**（用於 OCR 文字辨識）
   - 需要 Google Cloud 帳號
   - 需要建立專案並啟用 Vision API
   - 需要產生 JSON 金鑰檔案

2. **AI 翻譯服務**（選用，用於即時翻譯）
   - OpenAI API (GPT-4, GPT-3.5)
   - Anthropic Claude API
   - Google Gemini API
   - xAI Grok API

---

## 3. 安裝步驟

Kotoba 提供兩種安裝方式，請根據您的需求選擇：

### 方式 1：安裝版（推薦）

**適合對象**：一般使用者，想要快速安裝並在開始功能表建立捷徑

1. 前往 [Releases](https://github.com/meowhoo/kotoba/releases) 頁面
2. 下載最新版本的安裝程式：`Kotoba-0.1.0-Setup.exe`
3. 雙擊執行安裝程式
4. 選擇安裝位置（預設：`C:\Program Files\Kotoba`）
5. 完成安裝後，應用程式會自動啟動
6. 開始功能表會自動建立 `Kotoba` 捷徑

> **提示**：
> - 第一次執行時，Windows Defender 可能會顯示警告，請選擇「仍要執行」或「詳細資訊」→「仍要執行」
> - 安裝需要系統管理員權限
> - 安裝後可自訂桌面捷徑、開始功能表位置

### 方式 2：便攜版

**適合對象**：進階使用者，想要在 USB 隨身碟執行或不想安裝到系統

1. 前往 [Releases](https://github.com/meowhoo/kotoba/releases) 頁面
2. 下載便攜版壓縮檔：`Kotoba-0.1.0-win-x64.zip`
3. 解壓縮到您想要的位置（例如：`D:\Programs\Kotoba` 或 USB 隨身碟）
4. 進入資料夾，找到 `Kotoba.exe`
5. 雙擊啟動應用程式（無需安裝）

> **便攜版優點**：
> - 無需安裝，解壓即可使用
> - 可放在 USB 隨身碟，隨處執行
> - 所有設定檔、書籍資料都存放在應用程式資料夾內
> - 刪除資料夾即可完全移除

### 解除安裝

**安裝版**：
前往「設定」→「應用程式」→「已安裝的應用程式」→ 找到 `Kotoba` → 點擊「解除安裝」

**便攜版**：
直接刪除解壓縮的資料夾即可

---

## 4. 首次設定

### 步驟 1：設定 Google Cloud Vision API

首次啟動時，需要設定 Google Cloud Vision API 金鑰：

1. 進入書庫頁面後，點擊右上角的「系統設定」按鈕（⚙️）
2. 在「OCR 設定」區塊，找到「Google Vision API 憑證」
3. 點擊「📁」按鈕選擇您從 Google Cloud 下載的 JSON 金鑰檔案
4. 設定完成後，點擊「儲存設定」按鈕

> **如何取得 Google Cloud Vision API 金鑰？**
>
> 1. 前往 [Google Cloud Console](https://console.cloud.google.com/)
> 2. 建立新專案或選擇現有專案
> 3. **啟用付費帳戶**（必須，即使使用免費額度也需要設定信用卡）
> 4. 啟用「Cloud Vision API」
> 5. 前往「IAM 與管理」→「服務帳戶」
> 6. 建立服務帳戶並下載 JSON 金鑰檔案

### 步驟 2：設定 AI 翻譯服務（選用）

如果您需要使用即時翻譯功能：

1. 在「系統設定」中找到「AI 設定」區塊
2. 在「提供者」下拉選單選擇 AI 服務供應商（OpenAI / Anthropic / Google / xAI）
3. 在「API Key」欄位輸入對應的 API 金鑰
4. 在「模型」欄位輸入或選擇模型名稱（例如：gpt-4, claude-sonnet-4-20250514, gemini-2.0-flash-exp）
5. 點擊「儲存設定」按鈕

---

## 5. 功能詳解

### 5.1 書庫管理

書庫是您所有書籍的集中管理介面。

#### 新增書籍

1. 點擊左側導航列的「書庫」
2. 點擊右上角的「+ 新增書籍」按鈕
3. 填寫書籍資訊：
   - **書名**（必填）
   - **作者**（選填）
4. 點擊「確定」完成新增

> **提示**：書籍語言會在第一次 OCR 時自動偵測。

#### 設定封面圖片

1. 在書庫中找到要設定封面的書籍
2. 點擊書籍卡片右上角的「⋯」快捷選單
3. 選擇「封面」選項
4. 上傳圖片檔案（支援 JPG/PNG/WebP，大小 ≤ 10MB）
5. 系統會自動調整為 0.7:1 比例並儲存

#### 編輯書籍資訊

1. 在書庫中找到要編輯的書籍
2. 點擊書籍卡片右上角的「⋯」快捷選單，選擇「編輯」
3. 可修改以下內容：
   - **書名**：書籍標題
   - **作者**：作者名稱
   - **目標語言**：翻譯目標語言（支援：繁體中文、簡體中文、英文、韓文、日文、西班牙文、德文、法文、義大利文、葡萄牙文）
4. 修改完成後點擊「儲存」

> **提示**：變更目標語言後，新的翻譯將使用新語言，但已有的翻譯不會自動更新。

#### 刪除書籍

1. 點擊書籍卡片右上角的「⋯」快捷選單，選擇「刪除」
2. 確認刪除對話框
3. **注意**：刪除書籍會一併刪除該書所有頁面、OCR 結果和相關單字卡連結

#### 開始閱讀

**方式 1：雙擊書籍卡片**
- 雙擊書籍封面或卡片區域即可開啟閱讀頁面

**方式 2：使用 QuickMenu**
1. 點擊書籍卡片右上角的「⋯」快捷選單
2. 選擇「閱讀」選項

系統會記住您上次閱讀的進度。如果是第一次開啟且尚無頁面，需要先進行「抓圖設定」。

---

### 5.2 抓圖設定

抓圖功能讓您能夠快速截取書籍頁面圖片。

#### 設定 ROI（感興趣區域）

**什麼是 ROI？**

ROI (Region of Interest) 是您想要截取的固定區域，設定後可以使用全域快捷鍵快速截圖。

**設定步驟：**

1. 開啟書籍後，點擊頂部工具列的「抓圖」
2. 按下 `Ctrl+F9` 或點擊「設定 ROI」按鈕
3. 畫面會自動最小化，顯示紅色半透明選取框
4. 使用滑鼠拖曳選取您要截取的區域
5. 放開滑鼠後，應用程式會自動回到前台並顯示預覽
6. 確認無誤後，ROI 設定完成

#### 新增頁面

有兩種方式可以新增書籍頁面：

**方法 1：使用全域快捷鍵截圖**

1. 確保已設定 ROI
2. 開啟您的電子書軟體（如 Kindle、Adobe Reader）
3. 按下 `Ctrl+F10` 全域快捷鍵
4. 系統會自動截取 ROI 區域並儲存為新頁面

**方法 2：拖曳上傳圖片**

如果您已經有圖片檔案，可以直接拖曳上傳：

1. 在檔案總管中選擇圖片檔案
2. 拖曳到抓圖頁面的大圖區域
3. 放開滑鼠，圖片會自動加入到最後一頁
4. 系統支援 PNG、JPG、JPEG、WebP 格式，單檔最大 10MB
5. 一次只能上傳一張圖片

> **提示**：新上傳的圖片會自動編號並加在最後，無法插入到中間位置。

#### 圖片管理

**拖曳排序**

如果截圖順序不正確，可以直接拖曳調整：

1. 在左側縮圖列表中，點住要移動的圖片
2. 拖曳到目標位置
3. 放開滑鼠，系統會自動重新編號所有圖片檔案

**刪除圖片**

1. 點擊縮圖右上角的「⋯」更多選項按鈕
2. 選擇「刪除」
3. 確認後，系統會刪除圖片並自動重新編號

**鍵盤導航**

- 使用 `←` 和 `→` 方向鍵瀏覽圖片
- 系統會自動滾動到當前選中的圖片

---

### 5.3 OCR 辨識

OCR 功能會將截取的圖片轉換為可編輯的文字。

#### 執行 OCR

1. 點擊頂部工具列的「OCR」按鈕旁的範圍選擇下拉選單
2. 選擇辨識範圍：
   - **本頁**：只辨識當前顯示的頁面
   - **從此頁到結尾**：從當前頁面開始辨識到最後一頁
   - **全頁面**：辨識所有尚未 OCR 的頁面
3. 點擊「OCR」按鈕開始辨識
4. 系統會顯示進度條，可以隨時按 `Esc` 或點擊「取消」中斷作業
5. 在雙頁模式下，右側會即時顯示辨識的文字結果

#### 排除框（Exclusion Zones）

**解決傳統 OCR 的最大痛點**

書籍頁面的邊角經常有頁碼、頁眉、頁尾、書名等干擾文字，這些區域是傳統 OCR 工具的噩夢——它們會混入正文，打亂閱讀順序，甚至造成翻譯錯誤。

Kotoba 的排除框功能讓您可以精確標記這些不需要辨識的區域，系統會在 OCR 時自動跳過，徹底解決這個問題。系統提供兩種類型：

**全域排除框（紅色）**

適用於所有頁面的固定位置區域（例如：每頁都在同一位置的頁碼、頁眉）

1. 開啟「顯示排除」切換按鈕
2. 點擊底部工具列的「➕ 新增全域排除框」按鈕（或按 `G` 鍵）
3. 在預覽圖上拖曳選取要排除的區域
4. 確認後，該區域會以紅色邊框和淡紅遮罩標示
5. 此排除框會自動套用到所有頁面

**單頁排除框（橘色）**

僅套用到當前頁面的特殊區域（例如：某一頁的插圖、註解）

1. 開啟「顯示排除」切換按鈕
2. 點擊底部工具列的「➕ 新增單頁排除框」按鈕（或按 `P` 鍵）
3. 在預覽圖上拖曳選取要排除的區域
4. 確認後，該區域會以橘色邊框和淡橘遮罩標示
5. 此排除框只對當前頁面生效

**進階功能**

- **複製/貼上排除框**：使用 `Ctrl+C` 複製當前頁的單頁排除框，切換到其他頁面後使用 `Ctrl+V` 貼上
- **忽略全域排除**：勾選「☑ 本頁不使用全域排除」核取方塊，當前頁面將不套用全域排除框
- **刪除排除框**：點選排除框後按 `Delete` 鍵刪除
- **編輯排除框**：點擊排除框的邊框或角落拖動調整大小和位置

設定完成後，重新執行 OCR 即可生效。

#### 多語言支援

Kotoba 支援多國語言的 OCR 辨識，系統會根據書籍設定的語言自動選擇對應的 OCR 引擎。

---

### 5.4 閱讀模式

閱讀模式提供三種顯示方式，可透過頂部工具列切換。

#### Image 模式（純圖片）

1. 點擊「Image」按鈕
2. 顯示書頁原圖
3. 將滑鼠移到圖片上的單字（hover），會顯示單字卡提示框
4. 點擊單字可以開啟完整的單字卡資訊

**適用情境**：想看原汁原味的書頁排版，同時快速查看已建立的單字卡。

#### Image+TL 模式（左圖右譯）

1. 點擊「Image+TL」按鈕
2. 畫面分為左右兩欄：左側顯示原圖，右側顯示 AI 翻譯
3. **智慧排版**：系統會自動判定原文方向，中日文以直排顯示，其他語言使用橫排
4. 可拖動中間分隔線調整圖片與翻譯的比例（20%-80%）
5. 如果這頁已經有 OCR 文字，可以點擊「翻譯」按鈕呼叫 LLM 進行翻譯
6. 翻譯結果會快取，下次開啟該頁時直接顯示

**適用情境**：想對照原圖與翻譯，理解整段文章的意思。直排顯示讓中日文閱讀更自然。

#### Text 模式（OCR 文字 + 單字列表）

1. 點擊「Text」按鈕
2. 左側顯示 OCR 辨識的文字
3. **智慧排版**：系統會自動判定文字方向，中日文以直排顯示，其他語言使用橫排
4. 右側顯示本頁出現的單字列表
5. 選取文字後，可以點擊「+ 單字卡」按鈕新增單字卡
6. 可調整字體大小（12-48px）

**適用情境**：想選取文字、複製內容，或新增單字卡。直排顯示讓中日文閱讀更自然。

#### 頁面導航

- **上一頁**: 點擊左側箭頭或按下 `左方向鍵`
- **下一頁**: 點擊右側箭頭或按下 `右方向鍵`
- **跳至指定頁**: 點擊頁碼輸入框，輸入頁碼後按 `Enter`

---

### 5.5 翻譯設定

每本書籍都可以設定專屬的翻譯風格、角色語氣和名詞對照表，讓 AI 翻譯更貼近您的需求。

#### 開啟翻譯設定

1. 在閱讀頁面，點擊頂部工具列的「翻譯設定」按鈕
2. 會開啟一個包含三個分頁的設定視窗：翻譯風格、角色設定、名詞設定

#### 翻譯風格設定

**Temperature（溫度值）**
- 控制翻譯的創意程度，範圍 0.0-1.0
- 較低值（如 0.3）：翻譯更精確、一致
- 較高值（如 0.8）：翻譯更自然、有變化
- 建議值：0.7

**敬語處理**（針對日文原文）
- **保留**：盡可能保留敬語的語氣
- **轉換**：轉換為現代中文的禮貌說法
- **省略**：轉換為不帶敬意的普通說法

**翻譯約束**
- 提供明確的翻譯規則給 AI，每行一條規則
- 可點擊預設範本按鈕（S1-S5）快速套用常見範本
- 範本包含：標準通用、口語敘事、文藝典雅、輕小說、技術文檔等
- 範本套用後可自由編輯

範例約束：
```
**語感/用詞**: 中性、清爽、不浮誇
**節奏/句長**: 中等句長;複句不超過 2–3 個分句
**對話/內心戲**: 對話自然但不過度口語
**標點/排版**: 採用專案標點規範;保留原段與台詞分行
```

#### 角色設定

為書中主要角色定義說話風格，AI 會在翻譯對話時模仿這些語氣。

1. 點擊「+ 新增角色」按鈕
2. 輸入角色名稱和語氣描述
3. 例如：
   - **主角**：「年輕直率，少用敬語，語氣輕鬆」
   - **老師**：「語氣沉穩、莊重含蓄，多用敬語」
4. 可隨時編輯或刪除角色

#### 名詞對照表

建立專有名詞的固定翻譯，確保全書一致性。

1. 點擊「+ 新增」按鈕
2. 填寫：
   - **原文**：書中出現的詞彙（例如：魔法）
   - **譯文**：希望翻譯成的文字（例如：Magic）

---

### 5.6 單字卡系統

單字卡系統是獨立於書籍的生字管理功能。

#### 新增單字卡

單字卡透過 AI 自動標註生成，包含發音、翻譯與例句。

1. 在 **Text 模式**下，選取要加入的單字或句子
2. 點擊「+ 單字卡」按鈕
3. 系統會自動呼叫 LLM 產生單字卡內容：
   - **單字/片語**：您選取的文字
   - **發音**：自動標註（如日文假名）
   - **翻譯**：AI 自動翻譯
   - **例句**：包含該單字的上下文
4. 單字卡會自動儲存並顯示在頁面上

#### 刪除單字卡

1. 點擊單字卡右側的「刪除」圖示
2. 確認刪除

#### 搜尋與篩選

- **搜尋**：在上方搜尋框輸入關鍵字，可以搜尋單字、翻譯、例句
- **依書籍篩選**：點擊篩選下拉選單，選擇特定書籍，只顯示該書的單字卡

#### 匯出 Anki 卡牌

您可以將書籍的單字卡匯出為 Anki `.apkg` 格式，方便在 Anki 中複習。

**操作步驟：**

1. 在書庫頁面，找到要匯出單字卡的書籍
2. 點擊書籍卡片右上角的「⋯」快捷選單
3. 選擇「匯出 Anki」
4. 點擊「匯出」按鈕
5. 系統會自動產生 `.apkg` 檔案並儲存到您選擇的位置
6. 開啟 Anki，選擇「檔案」→「匯入」，選取剛才產生的 `.apkg` 檔案即可

---

## 6. 快捷鍵參考

### 全域快捷鍵（應用程式最小化也有效）

| 快捷鍵 | 功能 |
|--------|------|
| `Ctrl+F9` | 設定 ROI（感興趣區域） |
| `Ctrl+F10` | 快速截圖並新增頁面 |

### 閱讀頁面快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| `←` | 上一頁 |
| `→` | 下一頁 |

### 通用快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| `F11` | 全螢幕模式 |

---

## 7. 常見問題

### Q1: 首次執行時 Windows 出現安全警告怎麼辦？

這是正常現象，因為軟體未經 Microsoft 程式碼簽章認證。請依照系統版本處理：

**Windows 10 / 11 - SmartScreen 警告**
1. 出現「Windows 已保護您的電腦」或「Microsoft Defender SmartScreen 已封鎖無法識別的應用程式」
2. 點擊「更多資訊」（More info）
3. 點擊「仍要執行」（Run anyway）

**Windows 11 - Smart App Control（較嚴格）**

如果無法透過「仍要執行」繞過，表示啟用了 Smart App Control：
1. 開啟「設定」（Windows + I）
2. 前往「隱私權與安全性」→「Windows 安全性」→「應用程式與瀏覽器控制」
3. 點擊「Smart App Control 設定」
4. 選擇「關閉」（注意：關閉後需重設 Windows 才能重新開啟）

**替代方案：解除封鎖檔案**
1. 在 `Kotoba.exe` 上按右鍵 → 內容
2. 在「一般」分頁底部勾選「解除封鎖」（Unblock）
3. 按「套用」→「確定」

### Q2: 如果我無法成功使用 Ctrl+F10 截圖怎麼辦？

**解決方案：使用拖曳上傳功能**

如果您遇到快捷鍵截圖問題（例如：快捷鍵衝突、無法設定 ROI、截圖失敗等），可以改用拖曳上傳的方式：

1. **手動截圖或取得圖片**
   - 使用 Windows 內建的「剪取工具」（Win + Shift + S）截取書頁
   - 或使用其他截圖軟體（如 Snagit、ShareX）
   - 或直接使用已有的書頁圖片檔案

2. **儲存為圖片檔案**
   - 將截圖儲存為 PNG、JPG 或 WebP 格式
   - 確保檔案大小不超過 10MB

3. **拖曳上傳到 Kotoba**
   - 開啟 Kotoba 的「抓圖」頁面
   - 在檔案總管中找到圖片檔案
   - 直接拖曳到抓圖頁面的大圖區域
   - 放開滑鼠，圖片會自動加入書籍

4. **重複操作**
   - 每張圖片需要單獨拖曳上傳（一次只能上傳一張）
   - 系統會自動編號並加在最後一頁

**優點**：
- 不需要設定 ROI
- 不依賴全域快捷鍵
- 可以使用任何截圖工具
- 適合處理已有的圖片檔案

### Q3: OCR 辨識結果不準確怎麼辦？

**可能原因：**
- 圖片解析度過低
- 圖片包含過多雜訊（背景花紋、水印）

**解決方法：**
1. 請確保圖片解析度至少 1024x768 以上
2. 確保截圖時選取的區域完整且清晰
3. 使用「排除框」功能排除頁碼、插圖等干擾區域

### Q4: 如果我已經有一本書的完整圖檔，不想一張一張拖曳上傳，該怎麼做？

**答案：直接放入書籍資料夾**

如果您已經有整本書的圖片檔案，可以直接複製到書籍的 `images/` 目錄：

**操作步驟：**

1. **找到書籍資料夾**
   - 預設位置：`C:\Users\您的使用者名稱\AppData\Roaming\kotoba\data\books\{書籍ID}`
   - 或在「系統設定」→「資料夾路徑」中查看自訂路徑

2. **準備圖片檔案**
   - 將圖片檔案重新命名為：`page_0001.png`、`page_0002.png`、`page_0003.png`...
   - **注意**：檔案名稱格式必須是 `page_` + 4位數字 + `.png`
   - **只支援 PNG 格式**（如果是 JPG 或其他格式，需先轉換為 PNG）

3. **複製到 images 目錄**
   - 將所有重新命名的圖片檔案複製到 `books\{書籍ID}\images\` 目錄下

4. **重新啟動應用程式**
   - 關閉 Kotoba
   - 重新開啟應用程式
   - 打開該書籍，即可看到所有圖片

**範例：**
```
books/my-book-123/images/
├── page_0001.png
├── page_0002.png
├── page_0003.png
├── page_0004.png
└── ...
```

> **提示**：如果圖片數量很多，可以使用批次重新命名工具（如 Windows PowerShell、Total Commander 等）來加快處理速度。

### Q5: AI 翻譯失敗怎麼辦？

**可能原因：**
- API 金鑰無效或已過期
- API 額度用盡
- 網路連線問題

**解決方法：**
1. 前往「系統設定」檢查 API 金鑰是否正確
2. 檢查您的 API 帳號是否還有餘額
3. 嘗試切換其他 AI 服務供應商

### Q6: 為什麼快捷鍵無效？

**可能原因：**
- 其他應用程式佔用了相同的快捷鍵
- 應用程式處於背景時未正確註冊快捷鍵

**解決方法：**
1. 確認沒有其他軟體使用 `Ctrl+F9` 或 `Ctrl+F10`
2. 重新啟動應用程式
3. 檢查 Windows 設定中是否有衝突的快捷鍵設定

### Q7: 書籍刪除後可以復原嗎？

**答案：不行**

刪除書籍是永久性操作，會一併刪除：
- 所有頁面圖片
- OCR 辨識結果
- 翻譯快取
- 相關單字卡的書籍連結（單字卡本身會保留）

建議在刪除前先確認是否真的不需要該書的資料。

### Q8: 可以在多台電腦同步資料嗎？

**可以，透過雲端同步軟體實現**

Kotoba 本身沒有內建雲端同步功能，但您可以透過以下方式在多台電腦間共用書庫：

**設定步驟：**
1. 在您的雲端同步資料夾中建立一個目錄（例如：`D:\Dropbox\Kotoba\data` 或 `D:\Google Drive\Kotoba\data`）
2. 在「系統設定」→「資料夾路徑」中，將書籍存放目錄指向該雲端同步資料夾
3. 在其他電腦上重複步驟 2，指向同一個雲端資料夾路徑

這樣一來，Google Drive、Dropbox、OneDrive 等雲端同步軟體就會自動同步您的書庫、閱讀進度和單字卡，讓您在多台電腦中使用同一個書庫。

---

## 8. 疑難排解

### 應用程式無法啟動

1. 確認作業系統符合需求（Windows 10/11 64-bit）
2. 檢查是否有防毒軟體阻擋
3. 嘗試「以系統管理員身分執行」
4. 查看 `debug.log`（位於 `C:\Users\YourUsername\AppData\Roaming\kotoba\debug.log`）

### OCR 一直失敗

1. 確認 Google Cloud Vision API 金鑰是否有效
2. 檢查 Google Cloud 專案是否已啟用 Vision API
3. 確認帳號是否有剩餘額度
4. 檢查網路連線是否正常

### 翻譯速度很慢

1. 翻譯速度取決於 AI 服務供應商的回應速度
2. GPT-4 通常比 GPT-3.5 慢，但品質較好
3. 可以考慮切換到 Claude 或 Gemini
4. 翻譯結果會快取，第二次開啟同一頁時會立即顯示

### 應用程式關閉後仍有殘留程序

1. 開啟「工作管理員」（`Ctrl+Shift+Esc`）
2. 找到 `Kotoba.exe` 或 `python.exe` 程序
3. 點擊「結束工作」
4. 如果問題持續，請回報給開發團隊

---

## 💡 使用技巧

### 高效截圖工作流程

1. 設定好 ROI 後，可以連續按 `Ctrl+F10` 快速截取多頁
2. 截圖時建議將電子書軟體設定為「單頁顯示」模式

### 提升 OCR 準確率

1. 截圖時盡量選擇原圖尺寸，避免縮放
2. 使用「排除框」功能排除頁碼、註解
3. 如果 OCR 結果有固定錯誤模式，可以手動編輯 TOML 檔案


---

---

**感謝您使用 Kotoba！**

祝您語言學習愉快！ 📚✨
